import {ConnectionConfig} from "mysql";

const config:ConnectionConfig = {
    host: "localhost",
    port: 3306,
    database: "bill",
    user: "root",
    password: "123456"
};
export default config;
